FROM node:20-slim

WORKDIR /app

# Copy package files first for better caching
COPY package*.json ./
RUN npm install

# Copy application code
COPY . .

# Expose port
EXPOSE 5847

# Run the development server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5847"]
